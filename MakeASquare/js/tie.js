// tie function


function ties(dotTrackerUser, dotTrackerComputer) {
    if (dotTrackerUser.length + dotTrackerComputer.length === 50) {
        return true;
    } else {
        return false;
    }
  }
